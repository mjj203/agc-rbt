[uwsgi]
chdir = /mapproxy
wsgi-file = /mapproxy/app.py
pyargv = /mapproxy/config/mapproxy.yaml
; http-socket (uWSGI's built-in HTTP router) rather than the raw uwsgi wire
; protocol (socket) -- this lets mapproxy be reached directly over plain
; HTTP, either by nginx's proxy_pass (see nginx/config/nginx.conf) or by an
; AWS ALB/CloudFront target when running without nginx (see deploy.sh --no-nginx).
http-socket = 0.0.0.0:5000
master = true
; UWSGI_PROCESSES/UWSGI_THREADS are set in docker-compose.yaml (see .env.example)
processes = $(UWSGI_PROCESSES)
threads = $(UWSGI_THREADS)
vacuum = true
max-requests = 20000
harakiri = 75
post-buffering = 65536
buffer-size = 65536
socket-timeout = 30
thunder-lock = true
enable-threads = true
lazy-apps = true
listen = 4096
cheaper = 4
cheaper-algo = busyness
cheaper-step = 2
cheaper-overload = 2
cheaper-busyness-multiplier = 30
cheaper-busyness-min = 20
cheaper-busyness-max = 80
cheaper-busyness-backlog-alert = 16
cheaper-busyness-backlog-step = 2
memory-report = true
log-slow = 200
log-4xx = true
log-5xx = true
; 4096/2048 assumes UWSGI_PROCESSES=8 on a 16GB host; tune both together
limit-as = 4096
reload-on-rss = 2048
worker-reload-mercy = 30
die-on-term = true
